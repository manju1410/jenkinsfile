node{
   stage('scm checkout'){
       git credentialsId: '', url: ''
   }    
   stage('Mvn package'){
     def mvnHome = tool name: 'maven-3', type: 'maven'
     sh "${mvnHome}/bin/mvn clean package"
   }
   stage('Deploy'){
      ws(''){
      sh 'cp -r *.war '
   }
  }
 } 
